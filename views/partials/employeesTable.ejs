<h3>Employee list</h3>
<table>
    <tr>
        <th>Name</th>
        <th>Address</th>
    </tr>
    <% if (typeof employees == 'object' && employees) { %>
    <% employees.forEach((employee, index) => { %>
    <tr class="<%= (index % 2 == 0) ? 'odd' : 'even' %>">
        <td><a href="/api/employees/<%= employee.empId %>/expenses"><label><%= employee.name %></label></a></td>
        <td><a href="/api/employees/<%= employee.empId %>/expenses"><label><%= employee.address %></label></a></td>
    </tr>
    <% }); %>
    <% } %>
</table>